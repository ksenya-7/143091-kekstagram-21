"use strict";

(() => {
  // const currentblockPictures = window.filter.blockPictures;
  // // console.log(currentblockPictures);

  // // (window.backend.load(openBigPicture, () => {}));

  // (window.backend.load((pictures) => {
  //   for (let i = 0; i < pictures.length; i++) {
  //     pictures[i].addEventListener(`click`, () => {
  //       console.log(pictures[i]);
  //       window.preview.renderBigPicture(pictures[i]);
  //     });
  //   }
  // }, () => {}));
})();
